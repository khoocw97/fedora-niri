modules:
  - type: files
    files:
      - source: system
        destination: /

  - type: script
    scripts:
      - setup-rime-build
      - setup-home-layout
      - install-megacmd
      - install-sysc-greet

  - type: systemd
    system:
      enabled:
        - greetd
    user:
      enabled:
        - mega-cmd-server
#        - kdeconnectd

  - type: justfiles
    install: true


